import React from "react";

import { MaterialCommunityIcons } from "@expo/vector-icons";

function BottomIcon({ name, color }) {
  return <MaterialCommunityIcons name={name} size={30} color={color} />;
}

export default BottomIcon;
